# ============================================
# Docker build context ignore (monorepo root)
# 所有 apps/*/Dockerfile 在根目录构建时统一使用此文件
# 目的：缩小 build context，稳定 COPY 层 hash，提升缓存命中率
# ============================================

# ---- 依赖目录（每次 install 后内容都会变） ----
node_modules
**/node_modules

# ---- 包管理器本地存储（巨大且无需进入容器） ----
.pnpm-store
.pnpm
.yarn

# ---- 构建产物 / 缓存（容器内会重新构建） ----
dist
**/dist
dist-ssr
distvite
dev-dist
build
**/build
.turbo
**/.turbo
*.tsbuildinfo
**/*.tsbuildinfo

# ---- 日志 ----
*.log
logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

# ---- Git / 版本控制 ----
.git
.gitignore
.gitattributes

# ---- 编辑器 / IDE / 工具配置（与构建无关） ----
.vscode
.idea
.cursor
.codebuddy
.kiro
.cspell
.husky
.changeset
.github

# ---- 系统文件 ----
.DS_Store
Thumbs.db
*.swp
*.swo

# ---- 文档（发包/构建不需要） ----
*.md
!packages/*/README.md

# ---- 本地环境与临时文件 ----
*.local
.env.local
.env.*.local
tmp
temp

# ---- 脚本（本地运维用，构建不需要） ----
auto_release_tag.sh
**/auto_release_tag.sh

# ---- vite 打包分析产物 ----
stats.html

# ---- Docker 自身 ----
**/Dockerfile.debug
**/.dockerignore
