重要原则：

- 优先保证 correctness，再考虑 elegance。
- 优先选择简单且可验证的设计。
- 不要为了减少代码量牺牲工程质量。
- 不要为了所谓“生产级”进行过度设计。
- 不确定时先阅读现有代码和文档，不要猜测。
- 修改前理解上下文，修改后检查完整 diff。
- 每次改动保持 scope 最小。
- 测试失败时继续定位问题，不要绕过测试。
- 不允许通过删除测试、降低断言或忽略错误来让测试通过。
- 如果发现现有架构存在问题，先说明原因，再决定是否重构。